# 亲密圈功能分支同步检查清单

## 📋 每周同步任务（每周一执行）

**当前分支**: `feature/intimate-circle`  
**目标分支**: `master`  
**开发周期**: Week 1 - Week 4（2026-01-29 至 2026-02-26）

---

## ✅ 同步步骤

### 1. 确认当前在功能分支
```bash
git branch  # 确保显示 * feature/intimate-circle
```

### 2. 提交当前工作
```bash
# 如果有未提交的改动
git add .
git commit -m "wip: 保存当前工作进度"
```

### 3. 拉取主分支最新代码
```bash
git pull origin master
```

### 4. 解决冲突（如果有）
- 逐个文件检查冲突标记 `<<<<<<<`、`=======`、`>>>>>>>`
- 手动解决后提交
```bash
git add .
git commit -m "chore: 同步 master 最新代码"
```

### 5. 推送到远程
```bash
git push origin feature/intimate-circle
```

---

## 📅 同步记录

| 日期 | 同步的 master 提交 | 是否有冲突 | 备注 |
|------|-------------------|-----------|------|
| 2026-02-03（Week 2） | - | - | |
| 2026-02-10（Week 3） | - | - | |
| 2026-02-17（Week 4） | - | - | |
| 2026-02-24（最终合并前） | - | - | |

---

## ⚠️ 注意事项

### 不要合并的文件（如果冲突）
- ✅ **保留功能分支版本**：
  - `backend/app/routers/circle.py`（新增文件）
  - `mobile/src/screens/CircleListScreen.tsx`（新增文件）
  - 其他亲密圈相关新文件

- ⚠️ **谨慎处理**：
  - `mobile/src/navigation/AppNavigator.tsx`（可能双方都改）
  - `mobile/src/i18n/zh.ts` 和 `en.ts`（需要合并双方的翻译）
  - `backend/app/main.py`（路由注册可能冲突）

### 如果冲突太多无法解决
```bash
# 方案1：撤销合并，稍后再试
git merge --abort

# 方案2：记录冲突文件，向 Claude 寻求帮助
git status > conflicts.txt
```

---

## 🚨 紧急情况处理

### 场景1：误在功能分支发布了
```bash
# 立刻回退
git reset --hard HEAD~1
git push origin feature/intimate-circle --force
```

### 场景2：需要紧急修复 master 的 bug
```bash
# 1. 暂存当前工作
git stash

# 2. 切换到 master
git checkout master
git pull origin master

# 3. 修复bug，提交，推送
git add .
git commit -m "fix: 紧急修复xxx"
git push origin master

# 4. 回到功能分支
git checkout feature/intimate-circle
git stash pop
```

---

## 📞 需要帮助时

如果遇到无法解决的问题，提供以下信息给 Claude：

1. 执行 `git status` 的输出
2. 冲突文件的内容
3. 你想保留哪个版本的改动

---

**创建日期**: 2026-01-29  
**负责人**: @dengdan  
**预计合并日期**: 2026-02-26（Week 4 结束）
